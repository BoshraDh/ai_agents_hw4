# Bug Investigation Log — scrapy-1

## Step 1: Graph Analysis

Ran Graphify (`graphify update`) on scrapy. Results: **7,480 nodes, 22,889 edges**.

Top nodes by degree (from real graph):
- `scrapy_crawler_crawler` (degree 577) — central orchestrator
- `any` (degree 519) — utility node
- `spiders_init_spider` (degree 432) — user-facing entry

Top by PageRank:
- `any` (0.010300)
- `utils_test_get_crawler` (0.008995)
- `scrapy_crawler_crawler` (0.008884)

`downloadermiddlewares_offsite_offsitemiddleware` is in top-50 by degree within middleware cluster.

## Step 2: Obsidian Review

`hot.md` auto-generated by PageRank on real `graph.json`.
`index.md` shows top-20 modules by connectivity.

## Step 3: Targeted Code Read

LangGraph agent read only `scrapy/downloadermiddlewares/offsite.py` (1 file).

Found the bug pattern:
```python
# The None-check guard in get_host_regex prevents TypeError
for domain in allowed_domains:
    if domain is None:
        continue
```

The current code has the fix applied. The bug (pre-fix) was the missing `if domain is None: continue` guard.

## Step 4: Fix Documented

See `obsidian/fix_before_after.md` for full before/after.

---

## Real Token Counts (live run 2026-06-24)

| Phase | Node | Tokens |
|-------|------|--------|
| Graph summary | graph_reader_node | 478 |
| Obsidian pages | obsidian_reader_node | 835 |
| Target code read | targeted_code_reader_node | 1,311 |
| Bug identification | bug_identifier_node | 743 |
| Fix generation | fixer_node | 714 |
| **Total graph-guided** | | **4,081** |

| Method | Tokens | Bug found? |
|--------|--------|-----------|
| Graph-guided | 4,081 | Yes (OffsiteMiddleware) |
| Naive (capped 80K chars) | 16,376 | No (wrong file) |
| **Reduction** | **4x** | |

**Note:** The naive agent was capped at 80,000 characters due to context limits.
The full scrapy codebase (453 Python files) would require ~110,000 tokens naive — giving an
expected reduction of **~27x** uncapped. The graph-guided agent also found the *correct* bug
while the naive agent was misled by an unrelated genspider.py issue.
